{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://contracts.karyalay.in/mail/events/submission.accepted-v1.schema.json",
  "title": "submission.accepted v1 payload",
  "description": "Message accepted by Postfix submission boundary.",
  "$comment": "Payload for the `data` member of envelope-v1. Fields are the minimum semantic set from karyalay-mail repository-spec-v1.0 Appendix D; exact types are defined here because Appendix D delegates them to the generated schema. Additive optional fields may be introduced within version 1 where old consumers safely ignore them (Master §22.6); consumers ignore unknown additive fields (Repo 1 Appendix D.1), so this schema does not close the object.",
  "type": "object",
  "required": [
    "organisation_id",
    "mailbox_id",
    "submission_id",
    "smtp_queue_id",
    "accepted_at"
  ],
  "properties": {
    "organisation_id": {
      "type": "string",
      "format": "uuid",
      "$comment": "Type source: Master §6.3 canonical tenant identifier; Master §6.2 UUID."
    },
    "mailbox_id": {
      "type": "string",
      "format": "uuid",
      "$comment": "Type source: Master §6.3 canonical mailbox identifier."
    },
    "submission_id": {
      "type": "string",
      "format": "uuid",
      "$comment": "Type source: Repo 1 Appendix A.34 send_submission_records.id BINARY(16)."
    },
    "smtp_queue_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "description": "Postfix queue identifier for the accepted message. Opaque; not a UUID.",
      "$comment": "Type source: Repo 1 §23.1 / Master §30.3 queue-ID correlation model."
    },
    "accepted_at": {
      "type": "string",
      "format": "date-time",
      "$comment": "Type source: Repo 1 Appendix A.34 send_submission_records; Master §20.2."
    }
  }
}
